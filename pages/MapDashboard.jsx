import React from 'react';

export default function MapDashboard() {
  return (
    <div style={{ padding: '40px', backgroundColor: '#f0f0f0', borderRadius: '8px' }}>
      <h1>Map Dashboard</h1>
      <p>Your interactive map and tracking data will load here.</p>
      <div style={{ height: '300px', background: '#ddd', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        Map Placeholder
      </div>
    </div>
  );
}
